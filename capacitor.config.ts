import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Phần mềm quản lý Bạch Hổ Đường',
  webDir: 'dist'
};

export default config;
